# CLAUDE.md — 헬레나 OS 헌법

> 이 문서는 AI 에이전트(Claude Code)가 이 레포지토리에서
> 자율적으로 작업할 때 따라야 할 규칙과 패턴을 정의한다.

## 레포 정체성
- **목적:** 종교 판타지 채널 — 의심하는 토마스가 제3의 시각으로 진짜 신앙을 찾는 탐구·창작.
- **경계 (필수):**
  - 누나·조카 신원(이름·위치·사진) 절대 노출 금지.
  - 타 종교·교단 비하 금지.
  - 신학적 단정 표현 금지 ("전문가 확인 필요"로).

## 작업 원칙
- **커밋 자주, 작게**: 기능 단위로 쪼개서 커밋
- **설명 남겨라**: "왜"를 커밋 메시지에 포함
- **깨져도 괜찮다**: 일단 작동, 나중에 개선

## 파일 구조
- `index.html` = 랜딩 페이지 (루트)
- `docs/` = 기획/설계
- `data/` = JSON 저장소
- `scripts/` = 자동화 스크립트
- `assets/` = 이미지/CSS/JS

## AI 행동 규칙
- 작업 전 `git pull`로 최신 상태 확인
- 완료 후 `git push` 자동 실행
- 커밋 메시지에 작업 맥락 포함
